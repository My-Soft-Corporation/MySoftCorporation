@model IEnumerable<MySoft.Institute.Entities.Attendance.StudentAttendance>

@{
    ViewBag.Title = "Student Attendance";
}

<div class="container-fluid">
        <div class="card">
            <div class="card-header">
                <div class="card-title">
                    <a href="@Url.Action("GiveAttendance","StudentAttendance")" class="btn btn-primary"><i class="fa fa-check mr-1"></i> Take/Give</a></div>
            </div>
            <div class="card-body">
                @if (Model.Count() > 0)
                {
                        @Html.Partial("_List", Model)
                }
                else
                {
                    <div class="alert alert-info alert-dismissible"><strong>No!</strong>Attendance History Not Found!</div>
                }

            </div>
        </div>
    </div>

